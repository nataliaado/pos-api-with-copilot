{
  "name": "pos-api-with-copilot",
  "version": "1.0.0",
  "description": "API para automação de transferências e usuários.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha 'test/**/*.test.js' --timeout 10000",
    "test-controller": "mocha 'test/controller/**/*.test.js' --timeout 10000",
    "test-external": "mocha 'test/external/**/*.test.js' --timeout 10000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "sinon": "^17.0.1",
    "supertest": "^6.3.3"
  }
}
